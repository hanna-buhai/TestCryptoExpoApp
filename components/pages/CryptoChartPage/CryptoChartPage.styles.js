import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 8
  },
  graphContainer: {
    flexDirection: 'row',
    height: 300,
  }
})

export default styles
